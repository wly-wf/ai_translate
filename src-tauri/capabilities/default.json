{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "selection-float"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
