# AI Translate

## 鼠标选中文本翻译

鼠标选中文本后，应用会显示一个悬浮的翻译按钮。取词优先使用 Windows UI Automation，并沿鼠标命中元素、父级元素和焦点元素查找真实文本控件；目标控件不提供 UIA 选区时，才临时模拟 `Ctrl + C` 读取文本。兜底前会保存原剪贴板，读取后仅在剪贴板未被其他程序再次修改时恢复，避免覆盖用户的新内容。代码编辑器、终端、与本次释放位置不匹配的旧选区，以及无法安全识别的高权限窗口均不会启用剪贴板兜底。

当前版本只实现文本取词，不包含截图 OCR；OCR 保留为未来覆盖画布、图片和远程桌面等不可访问文本场景的补充方向。

Windows 11 上的轻量级多模型翻译工具。使用 Tauri 2、Rust 与 React 构建。

## 已实现

- 鼠标选中文本后显示悬浮翻译按钮；新选区会移动并更新同一个按钮。
- 托盘图标右键菜单新增“划词翻译”开关，可随时切换；关闭后选中文本不再出现悬浮翻译按钮。
- 可在设置中同时启用 DeepSeek、Xiaomi MiMo、阿里云百炼、智谱开放平台、Moonshot 或一个自定义 OpenAI 兼容接口；翻译时会并发请求所有启用模型，哪个模型先完成就先展示哪个结果。翻译请求对 DeepSeek、Xiaomi MiMo 和阿里云百炼发送关闭思考参数；其他接口使用供应商默认行为，不保证关闭思考。
- 无边框、置顶、可隐藏的翻译结果浮窗；可选中文本后使用 Ctrl+C 或右键复制译文。
- 手动输入翻译，便于在未复制文本时使用。
- API Key 使用 Windows Credential Manager 保存，不写入项目文件或浏览器本地存储。
- 翻译提示词要求完整翻译口语、习语和引用的正文，并在输出前自查。英文结果会检查中文残留；只有与原文完全一致的反引号代码、HTTP(S) 网址和占位符可保留中文，中文名称使用英文名称或拼音。发现残留时自动纠正一次，仍不合格则显示错误。正常结果不增加请求，纠正最多额外调用一次；此检查用于防止漏翻的中文直接展示，不保证译文语义完全准确。

## 运行

```powershell
cd D:\AI\ai_project\ai_translate\translator
npm.cmd run tauri dev
```

首次打开后，点击右上角菜单，在“供应商”中保存 API Key、URL 和模型名称，再点击“加入翻译”。可以用相同方式启用多个模型；之后选中文本并点击悬浮翻译按钮，即可同时查看多个模型的译文，也可以从悬浮翻译窗口进入快速翻译并手动输入文本。

## 验证

```powershell
npm.cmd run build
cd src-tauri
cargo check
```

## Windows 11 手动验收

以下桌面兼容性项目需要在没有其他进程占用 Vite `1420` 端口时运行 `npm.cmd run tauri dev` 验证；当前状态为待验收：

| 场景 | 状态 |
| --- | --- |
| 记事本单行选区 | 待验收 |
| 浏览器文本选区 | 待验收 |
| 跨行选区与最后一行定位 | 待验收 |
| 屏幕边缘位置夹紧 | 待验收 |
| 新选区替换旧选区 | 待验收 |
| 空白点击隐藏悬浮按钮 | 待验收 |
| 点击悬浮按钮翻译并显示结果或错误 | 待验收 |

管理员权限窗口受 Windows UIPI 限制，只记录实际表现，不作为通过条件。

## 接口与保存行为

当前支持一个自定义 OpenAI 兼容接口，重复添加会打开已有接口供编辑；不会覆盖已有凭据。Gemini/Claude 原生协议未实现，需自行使用兼容网关。模型列表至少保留一个已配置模型，停止调用请关闭供应商开关。API 请求不跟随重定向，请填写最终接口地址。翻译最多同时请求 4 个模型，新批次取消旧批次的本地任务。
